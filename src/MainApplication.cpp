#include "MainApplication.h"

namespace TerClone
{
    MainApplication::MainApplication(const ApplicationWindowSpec &spec): Application(spec)
    {
    }

    void MainApplication::Update(const sf::Time deltaTime)
    {

    }

    void MainApplication::Render()
    {
    }
}
